
import { RegisterForm } from '@/components/auth/register-form'
import React from 'react'

interface Props {
    
}

const page = (props: Props) => {
    return (
        <div>
          <RegisterForm/> 
        </div>
    )
}

export default page
