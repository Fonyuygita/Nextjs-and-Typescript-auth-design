import { LoginForm } from '@/components/auth/login-form'
import React from 'react'

interface Props {
    
}

const page = (props: Props) => {
    return (
        <div>
          <LoginForm/> 
        </div>
    )
}

export default page
